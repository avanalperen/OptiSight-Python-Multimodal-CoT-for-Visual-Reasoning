# MODEL_PATH defines the location of the Qwen2-VL model directory.
# If not set, the application will default to looking in ./models/qwen2-vl-2b-instruct or similar relative paths if coded,
# otherwise it might fail. Only absolute paths recommended.

MODEL_PATH=/path/to/your/models/qwen2-vl-2b-instruct
